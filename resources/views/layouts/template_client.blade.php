<!--
Author: W3layouts
Author URL: http://w3layouts.com
-->
<!doctype html>
<html lang="zxx">


<!-- Mirrored from p.w3layouts.com/demos_new/template_demo/06-11-2024/shipper-liberty-demo_Free/1152162366/web/index.html by HTTrack Website Copier/3.x [XR&CO'2014], Thu, 01 May 2025 18:06:48 GMT -->
<!-- Added by HTTrack --><meta http-equiv="content-type" content="text/html;charset=UTF-8" /><!-- /Added by HTTrack -->
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="keywords"
          content="Shipper Responsive web template, Bootstrap Web Templates, Android Compatible web template, Smartphone Compatible web template, free webdesigns for Nokia, Samsung, LG, SonyEricsson, Motorola web design" />
    <title>Shipper Transportation Category Bootstrap Responsive Web Template | Home </title>
    <link href="http://fonts.googleapis.com/css2?family=Hind+Siliguri:wght@300;400;600;700&amp;display=swap"
          rel="stylesheet">
    <!-- Template CSS -->
    <link rel="stylesheet" href="{{asset("assets/css/style-liberty.css")}}">
    <style>
    /*   #map-container {
            height: 300px;
            width: 100%;
            border-radius: 0.5rem;
            margin-top: 1rem;
            border: 1px solid #e2e8f0;
        }
        .pac-container {
            z-index: 1050 !important;
        }
        -->

     */
    .map-box {
        height: 300px;
        width: 100%;
        border-radius: 0.5rem;
        margin: 1rem 0;
        border: 1px solid #e2e8f0;
    }

    #map-container {
        height: 300px;
        width: 100%;
        border-radius: 0.5rem;
        margin: 1rem 0;
        border: 1px solid #e2e8f0;
    }

    .pac-container {
        z-index: 1050 !important;
    }
    </style>
</head>

<body>
<script src="../../../../../../../m.servedby-buysellads.com/monetization.js" type="text/javascript"></script>
<script>
    (function(){
        if(typeof _bsa !== 'undefined' && _bsa) {
            // format, zoneKey, segment:value, options
            _bsa.init('flexbar', 'CKYI627U', 'placement:w3layoutscom');
        }
    })();
</script>
<script>
    (function(){
        if(typeof _bsa !== 'undefined' && _bsa) {
            // format, zoneKey, segment:value, options
            _bsa.init('fancybar', 'CKYDL2JN', 'placement:demo');
        }
    })();
</script>
<script>
    (function(){
        if(typeof _bsa !== 'undefined' && _bsa) {
            // format, zoneKey, segment:value, options
            _bsa.init('stickybox', 'CKYI653J', 'placement:w3layoutscom');
        }
    })();
</script>
<!-- Global site tag (gtag.js) - Google Analytics -->
<script async src='https://www.googletagmanager.com/gtag/js?id=G-98H8KRKT85'></script>
<script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-98H8KRKT85');
</script>
@yield('content')

<meta name="robots" content="noindex">
<link rel="stylesheet" href="../../../../../../assests/css/font-awesome.min.css">
<!-- New toolbar-->
<style>
    * {
        box-sizing: border-box;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen-Sans, Ubuntu, Cantarell, "Helvetica Neue", sans-serif;
    }


    #w3lDemoBar.w3l-demo-bar {
        top: 0;
        right: 0;
        bottom: 0;
        z-index: 9999;
        padding: 40px 5px;
        padding-top:70px;
        margin-bottom: 70px;
        background: #0D1326;
        border-top-left-radius: 9px;
        border-bottom-left-radius: 9px;
    }

    #w3lDemoBar.w3l-demo-bar a {
        display: block;
        color: #e6ebff;
        text-decoration: none;
        line-height: 24px;
        opacity: .6;
        margin-bottom: 20px;
        text-align: center;
    }

    #w3lDemoBar.w3l-demo-bar span.w3l-icon {
        display: block;
    }

    #w3lDemoBar.w3l-demo-bar a:hover {
        opacity: 1;
    }

    #w3lDemoBar.w3l-demo-bar .w3l-icon svg {
        color: #e6ebff;
    }
    #w3lDemoBar.w3l-demo-bar .responsive-icons {
        margin-top: 30px;
        border-top: 1px solid #41414d;
        padding-top: 40px;
    }
    #w3lDemoBar.w3l-demo-bar .demo-btns {
        border-top: 1px solid #41414d;
        padding-top: 30px;
    }
    #w3lDemoBar.w3l-demo-bar .responsive-icons a span.fa {
        font-size: 26px;
    }
    #w3lDemoBar.w3l-demo-bar .no-margin-bottom{
        margin-bottom:0;
    }
    .toggle-right-sidebar span {
        background: #0D1326;
        width: 50px;
        height: 50px;
        line-height: 50px;
        text-align: center;
        color: #e6ebff;
        border-radius: 50px;
        font-size: 26px;
        cursor: pointer;
        opacity: .5;
    }
    .pull-right {
        float: right;
        position: fixed;
        right: 0px;
        top: 70px;
        width: 90px;
        z-index: 99999;
        text-align: center;
    }
    /* ============================================================
    RIGHT SIDEBAR SECTION
    ============================================================ */

    #right-sidebar {
        width: 90px;
        position: fixed;
        height: 100%;
        z-index: 1000;
        right: 0px;
        top: 0;
        margin-top: 60px;
        -webkit-transition: all .5s ease-in-out;
        -moz-transition: all .5s ease-in-out;
        -o-transition: all .5s ease-in-out;
        transition: all .5s ease-in-out;
        overflow-y: auto;
    }

    /* ============================================================
    RIGHT SIDEBAR TOGGLE SECTION
    ============================================================ */

    .hide-right-bar-notifications {
        margin-right: -300px !important;
        -webkit-transition: all .3s ease-in-out;
        -moz-transition: all .3s ease-in-out;
        -o-transition: all .3s ease-in-out;
        transition: all .3s ease-in-out;
    }



    @media (max-width: 992px) {
        #w3lDemoBar.w3l-demo-bar a.desktop-mode{
            display: none;

        }
    }
    @media (max-width: 767px) {
        #w3lDemoBar.w3l-demo-bar a.tablet-mode{
            display: none;

        }
    }
    @media (max-width: 568px) {
        #w3lDemoBar.w3l-demo-bar a.mobile-mode{
            display: none;
        }
        #w3lDemoBar.w3l-demo-bar .responsive-icons {
            margin-top: 0px;
            border-top: none;
            padding-top: 0px;
        }
        #right-sidebar,.pull-right {
            width: 90px;
        }
        #w3lDemoBar.w3l-demo-bar .no-margin-bottom-mobile{
            margin-bottom: 0;
        }
    }
    input[type="checkbox"] {
        display: inline-block !important;
    }

</style>
<div class="pull-right toggle-right-sidebar">
    <span class="fa title-open-right-sidebar tooltipstered fa-angle-double-right"></span>
</div>

<div id="right-sidebar" class="right-sidebar-notifcations nav-collapse">
    <div class="bs-example bs-example-tabs right-sidebar-tab-notification" data-example-id="togglable-tabs">

        <div id="w3lDemoBar" class="w3l-demo-bar">
            <div class="demo-btns">
                <a href="https://w3layouts.com/?p=0">
            <span class="w3l-icon -back">
                <span class="fa fa-arrow-left"></span>
            </span>
                    <span class="w3l-text">Back</span>
                </a>
                <a href="https://w3layouts.com/?p=0">
            <span class="w3l-icon -download">
                <span class="fa fa-download"></span>
            </span>
                    <span class="w3l-text">Download</span>
                </a>
                <a href="https://w3layouts.com/checkout/?add-to-cart=0" class="no-margin-bottom-mobile">
            <span class="w3l-icon -buy">
                <span class="fa fa-shopping-cart"></span>
            </span>
                    <span class="w3l-text">Buy</span>
                </a>
            </div>
            <!---<div class="responsive-icons">
                <a href="#url" class="desktop-mode">
                    <span class="w3l-icon -desktop">
                        <span class="fa fa-desktop"></span>
                    </span>
                </a>
                <a href="#url" class="tablet-mode">
                    <span class="w3l-icon -tablet">
                        <span class="fa fa-tablet"></span>
                    </span>
                </a>
                <a href="#url" class="mobile-mode no-margin-bottom">
                    <span class="w3l-icon -mobile">
                        <span class="fa fa-mobile"></span>
                    </span>
                </a>
            </div>-->
        </div>
        <div class="right-sidebar-panel-content animated fadeInRight" tabindex="5003"
             style="overflow: hidden; outline: none;">
        </div>
    </div>
</div>
</div>


<!--header-->
<header id="site-header" class="fixed-top">
    <!-- top header -->
    <section class="w3l-top-header">
        <div class="container">
            <div class="top-header-2">
                <div class="d-grid main-top">
                    <div class="top-headers">
                        <ul>
                            <li>
                                <span class="fa fa-phone mr-2"></span> <a href="tel:+1(21) 234 4567">+1(21) 234 557 4567</a>
                            </li>
                            <li>
                                <a href="#help" class="d-sm-block d-none">Help</a>
                            </li>
                        </ul>
                    </div>
                    <div class="top-headers ml-lg-4">
                        <ul>
                            <li>
                                <a href="#facebook"><span class="fa fa-facebook"></span></a>
                            </li>
                            <li>
                                <a href="#twitter"><span class="fa fa-twitter"></span></a>
                            </li>
                            <li>
                                <a href="#instagram"><span class="fa fa-instagram"></span></a>
                            </li>
                            <li class="mr-0">
                                <a href="#pinterest"><span class="fa fa-pinterest"></span></a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- //top header -->
    <div class="container">
        <nav class="navbar navbar-expand-lg stroke px-0 pt-lg-0">
            <h1><a class="navbar-brand" href="index.html">
                    Sh<span class="sub-logo">i</span>pper</span>
                    <span class="sub-title">Transport & Cargo</span>
                </a></h1>
            <!-- if logo is image enable this
        <a class="navbar-brand" href="#index.html">
            <img src="image-path" alt="Your logo" title="Your logo" style="height:35px;" />
        </a> -->
            <button class="navbar-toggler  collapsed bg-gradient" type="button" data-toggle="collapse"
                    data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-icon fa icon-expand fa-bars"></span>
                <span class="navbar-toggler-icon fa icon-close fa-times"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
                <ul class="navbar-nav ml-lg-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{asset("index.html")}}">Home <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{asset("about.html")}}">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{asset("services.html")}}">Services</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#Pages" id="navbarDropdown" role="button" data-toggle="dropdown"
                           aria-haspopup="true" aria-expanded="false">
                            Pages <span class="fa fa-angle-down"></span>
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown1">
                            <a class="dropdown-item" href="blog.html">Blog Posts</a>
                            <a class="dropdown-item" href="blog-single.html">Blog Single</a>
                            <a class="dropdown-item" href="landing-single.html">Landing Single</a>
                            <a class="dropdown-item" href="elements.html">Elements</a>
                            <a class="dropdown-item" href="email.html">Email Template</a>
                            <a class="dropdown-item" href="error.html">404 error page</a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href=""{{asset("contact.html")}}>Contact</a>
                    </li>
                </ul>

                <!--/search-right-->
                <div class="search-right">
                    <!--/search-right-->
                    <form class="search position-relative">
                        <input type="search" class="search__input" name="search" placeholder="Search here.." onload="equalWidth()"
                               required="">
                        <span class="fa fa-search search__icon"></span>
                    </form>

                    <!--//search-right-->
                </div>
                <!--//search-right-->

            </div>
            <!-- toggle switch for light and dark theme -->
            <div class="mobile-position">
                <nav class="navigation">
                    <div class="theme-switch-wrapper">
                        <label class="theme-switch" for="checkbox">
                            <input type="checkbox" id="checkbox">
                            <div class="mode-container">
                                <i class="gg-sun"></i>
                                <i class="gg-moon"></i>
                            </div>
                        </label>
                    </div>
                </nav>
            </div>
            <!-- //toggle switch for light and dark theme -->
        </nav>
    </div>
</header>
<script src="{{asset("assets/js/jquery-3.3.1.min.js")}}"></script>
<script src="{{asset("assets/js/theme-change.js")}}"></script>
<script src="{{asset("assets/js/owl.carousel.js")}}"></script><!-- owl carousel -->
<!-- script for tesimonials carousel slider -->
<script>
    $(document).ready(function () {
        $("#owl-demo1").owlCarousel({
            loop: true,
            margin: 20,
            nav: false,
            responsiveClass: true,
            responsive: {
                0: {
                    items: 1,
                    nav: false
                },
                1000: {
                    items: 1,
                    nav: false,
                    loop: false
                }
            }
        })
    })
</script>
<!-- //script for tesimonials carousel slider -->
<!-- footer -->
<section class="w3l-footer-29-main">
    <div class="footer-29 py-5">
        <div class="container py-lg-3">
            <div class="row footer-top-29">
                <div class="col-lg-3 col-sm-6 footer-list-29 footer-2 mt-lg-0 mt-md-5 mt-4">
                    <ul>
                        <h6 class="footer-title-29">Company</h6>
                        <li><a href="about.blade.php">About Us</a></li>
                        <li><a href="blog.blade.php"> Blog Posts</a></li>
                        <li><a href="services.blade.php">Services</a></li>
                        <li><a href="#careers"> Careers</a></li>
                        <li><a href="about.blade.php">Team</a></li>
                        <li><a href="contact.blade.php">Contact us</a></li>
                    </ul>
                </div>
                <div class="col-lg-3 col-sm-6 footer-list-29 footer-2 mt-lg-0 mt-md-5 mt-4">
                    <h6 class="footer-title-29">What We Do</h6>
                    <ul>
                        <li><a href="#ware">Warehousing</a></li>
                        <li><a href="#air">Air Freight</a></li>
                        <li><a href="#ocen">Ocean Freight</a></li>
                        <li><a href="#supply">Supply Chain</a></li>
                        <li><a href="#pack">Packaging</a></li>
                        <li><a href="#news">News & Media</a></li>
                    </ul>
                </div>
                <div class="col-lg-3 col-sm-6 footer-list-29 footer-2 mt-lg-0 mt-md-5 mt-4">
                    <h6 class="footer-title-29">Quick Links</h6>
                    <ul>
                        <li><a href="#terms">Terms of service</a></li>
                        <li><a href="#privacy"> Privacy policy</a></li>
                        <li><a href="#doc"> Documentation</a></li>
                        <li><a href="#support"> Support</a></li>
                        <li><a href="#components"> Components</a></li>
                        <li><a href="#changelog"> Changelog</a></li>
                    </ul>
                </div>
                <div class="col-lg-3 col-sm-6 footer-list-29 footer-2 mt-lg-0 mt-md-5 mt-4">
                    <div class="footer-logo mb-3">
                        <h2><a class="navbar-brand" href="index.html">
                                Sh<span class="sub-logo">i</span>pper</span>
                                <span class="sub-title">Transport & Cargo</span>
                            </a>
                        </h2>
                    </div>
                    <p>Lorem ipsum dolor sit amet consectetur ipsum elit.</p>
                    <ul class="mt-3">
                        <li><a href="tel:+(21) 255 999 8888"><span class="fa fa-phone"></span> +(21) 255 999 8888</a></li>
                        <li><a href="https://p.w3layouts.com/cdn-cgi/l/email-protection#81e4f9e0ecf1ede4c1ece0e8edafe2eeec" class="mail"><span class="fa fa-envelope-o"></span>
                                <span class="__cf_email__" data-cfemail="15706d74786579705578747c793b767a78">[email&#160;protected]</span></a></li>
                        <li><span class="fa fa-map-marker"></span>
                            Shipper, #2214 Honey street, NY - 62617.</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- //footer -->
<!-- copyright -->
<section class="w3l-copyright">
    <div class="container">
        <div class="row bottom-copies">
            <p class="col-lg-8 copy-footer-29">© 2021 Shipper. All rights reserved.Design by<a href="https://w3layouts.com/"
                                                                                               target="_blank">
                    W3layouts</a></p>
            <div class="col-lg-4 main-social-footer-29">
                <a href="#facebook" class="facebook"><span class="fa fa-facebook"></span></a>
                <a href="#twitter" class="twitter"><span class="fa fa-twitter"></span></a>
                <a href="#pinterest"><span class="fa fa-pinterest"></span></a>
                <a href="#instagram" class="instagram"><span class="fa fa-instagram"></span></a>
                <a href="#linkedin" class="linkedin"><span class="fa fa-linkedin"></span></a>
            </div>

        </div>
    </div>

    <!-- move top -->
    <button onclick="topFunction()" id="movetop" title="Go to top">
        &#10548;
    </button>
<script src="assets/js/jquery.magnific-popup.min.js"></script>
<script>
    $(document).ready(function () {
        $('.popup-with-zoom-anim').magnificPopup({
            type: 'inline',

            fixedContentPos: false,
            fixedBgPos: true,

            overflowY: 'auto',

            closeBtnInside: true,
            preloader: false,

            midClick: true,
            removalDelay: 300,
            mainClass: 'my-mfp-zoom-in'
        });

        $('.popup-with-move-anim').magnificPopup({
            type: 'inline',

            fixedContentPos: false,
            fixedBgPos: true,

            overflowY: 'auto',

            closeBtnInside: true,
            preloader: false,

            midClick: true,
            removalDelay: 300,
            mainClass: 'my-mfp-slide-bottom'
        });
    });
</script>

<!-- //magnific popup -->
<!-- stats number counter-->
<script src="assets/js/jquery.waypoints.min.js"></script>
<script src="assets/js/jquery.countup.js"></script>
<script>
    $('.counter').countUp();
</script>
<!-- //stats number counter -->
<!-- disable body scroll which navbar is in active -->
<script>
    $(function () {
        $('.navbar-toggler').click(function () {
            $('body').toggleClass('noscroll');
        })
    });
</script>
<!-- disable body scroll which navbar is in active -->

<!--/MENU-JS-->
<script>
    $(window).on("scroll", function () {
        var scroll = $(window).scrollTop();

        if (scroll >= 80) {
            $("#site-header").addClass("nav-fixed");
        } else {
            $("#site-header").removeClass("nav-fixed");
        }
    });

    //Main navigation Active Class Add Remove
    $(".navbar-toggler").on("click", function () {
        $("header").toggleClass("active");
    });
    $(document).on("ready", function () {
        if ($(window).width() > 991) {
            $("header").removeClass("active");
        }
        $(window).on("resize", function () {
            if ($(window).width() > 991) {
                $("header").removeClass("active");
            }
        });
    });
</script>

<!--//MENU-JS-->
<script src="assets/js/bootstrap.min.js"></script>


<script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'939135b6b87a44c2',t:'MTc0NjEyMjc1Ni4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='../../../../../../cdn-cgi/challenge-platform/h/b/scripts/jsd/a51d7b3d53cb/maind41d.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>


<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCg0hy8YfWY7LzfDyId8dd1e3FplF3msAY&libraries=places&callback=initMap" async defer></script>
<script>
    let pickupMap, dropoffMap;
    let pickupMarker, dropoffMarker;
    let pickupAutocomplete, dropoffAutocomplete;

    function initMap() {
        const defaultLoc = {lat: 33.8938, lng: 35.5018}; // Beirut

        // === Pickup Map ===
        pickupMap = new google.maps.Map(document.getElementById('pickup-map'), {
            center: defaultLoc,
            zoom: 12
        });

        pickupMarker = new google.maps.Marker({
            map: pickupMap,
            position: defaultLoc,
            draggable: true
        });

        pickupMap.addListener('click', function (event) {
            pickupMarker.setPosition(event.latLng);
            reverseGeocode(event.latLng, 'pickup');
            updateDistanceDisplay(); // ✅ added
        });

        pickupMarker.addListener('dragend', function (event) {
            reverseGeocode(event.latLng, 'pickup');
            updateDistanceDisplay(); // ✅ added
        });

        pickupAutocomplete = new google.maps.places.Autocomplete(document.getElementById("pickup-address"));
        pickupAutocomplete.addListener("place_changed", () => {
            const place = pickupAutocomplete.getPlace();
            if (!place.geometry) return;

            pickupMap.setCenter(place.geometry.location);
            pickupMarker.setPosition(place.geometry.location);
            fillHiddenFields(place.address_components, place.geometry.location, 'pickup');
            updateDistanceDisplay(); // ✅ already there
        });

        // === Dropoff Map ===
        dropoffMap = new google.maps.Map(document.getElementById('dropoff-map'), {
            center: defaultLoc,
            zoom: 12
        });

        dropoffMarker = new google.maps.Marker({
            map: dropoffMap,
            position: defaultLoc,
            draggable: true
        });

        dropoffMap.addListener('click', function (event) {
            dropoffMarker.setPosition(event.latLng);
            reverseGeocode(event.latLng, 'dropoff');
            updateDistanceDisplay(); // ✅ added
        });

        dropoffMarker.addListener('dragend', function (event) {
            reverseGeocode(event.latLng, 'dropoff');
            updateDistanceDisplay(); // ✅ added
        });

        dropoffAutocomplete = new google.maps.places.Autocomplete(document.getElementById("dropoff-address"));
        dropoffAutocomplete.addListener("place_changed", () => {
            const place = dropoffAutocomplete.getPlace();
            if (!place.geometry) return;

            dropoffMap.setCenter(place.geometry.location);
            dropoffMarker.setPosition(place.geometry.location);
            fillHiddenFields(place.address_components, place.geometry.location, 'dropoff');
            updateDistanceDisplay(); // ✅ already there
        });
    }

    function reverseGeocode(latlng, type) {
        const geocoder = new google.maps.Geocoder();
        geocoder.geocode({location: latlng}, (results, status) => {
            if (status === "OK" && results[0]) {
                document.getElementById(`${type}-address`).value = results[0].formatted_address;
                fillHiddenFields(results[0].address_components, latlng, type);
            }
        });
    }

    function getComponent(components, types) {
        for (let type of types) {
            const comp = components.find(c => c.types.includes(type));
            if (comp) return comp.long_name;
        }
        return '';
    }

    function fillHiddenFields(components, location, prefix) {
        document.getElementById(`${prefix}_city`).value = getComponent(components, ["locality", "administrative_area_level_2"]);
        document.getElementById(`${prefix}_state`).value = getComponent(components, ["administrative_area_level_1"]);
        document.getElementById(`${prefix}_country`).value = getComponent(components, ["country"]);
        document.getElementById(`${prefix}_street`).value = getComponent(components, ["route", "street_address"]);
        document.getElementById(`${prefix}_postal_code`).value = getComponent(components, ["postal_code"]);
        document.getElementById(`${prefix}_latitude`).value = location.lat();
        document.getElementById(`${prefix}_longitude`).value = location.lng();
    }

    function calculateDistance() {
        const pickupLat = parseFloat(document.getElementById("pickup_latitude").value);
        const pickupLng = parseFloat(document.getElementById("pickup_longitude").value);
        const dropoffLat = parseFloat(document.getElementById("dropoff_latitude").value);
        const dropoffLng = parseFloat(document.getElementById("dropoff_longitude").value);

        if (isNaN(pickupLat) || isNaN(pickupLng) || isNaN(dropoffLat) || isNaN(dropoffLng)) {
            return null;
        }

        const pickup = new google.maps.LatLng(pickupLat, pickupLng);
        const dropoff = new google.maps.LatLng(dropoffLat, dropoffLng);
        const distanceInMeters = google.maps.geometry.spherical.computeDistanceBetween(pickup, dropoff);
        return distanceInMeters / 1000; // in kilometers
    }

    function updateDistanceDisplay() {
        const distance = calculateDistance();
        const info = document.getElementById("distance-info");
        const hiddenInput = document.getElementById("calculated_distance");

        if (distance !== null) {
            const km = distance.toFixed(2);
            info.innerText = `Distance: ${km} km`;
            hiddenInput.value = km; // ✅ sets value in form
        } else {
            info.innerText = "Select both pickup and dropoff locations.";
            hiddenInput.value = '';
        }
    }


</script>

<!--<script>
    let pickupMap, dropoffMap;
    let pickupMarker, dropoffMarker;
    let pickupAutocomplete, dropoffAutocomplete;

    function initMap() {
        const defaultLoc = {lat: 33.8938, lng: 35.5018}; // Beirut

        // === Pickup Map ===
        pickupMap = new google.maps.Map(document.getElementById('pickup-map'), {
            center: defaultLoc,
            zoom: 12
        });

        pickupMarker = new google.maps.Marker({
            map: pickupMap,
            position: defaultLoc,
            draggable: true
        });

        pickupMap.addListener('click', function (event) {
            pickupMarker.setPosition(event.latLng);
            reverseGeocode(event.latLng, 'pickup');
        });

        pickupMarker.addListener('dragend', function (event) {
            reverseGeocode(event.latLng, 'pickup');
        });

        pickupAutocomplete = new google.maps.places.Autocomplete(document.getElementById("pickup-address"));
        pickupAutocomplete.addListener("place_changed", () => {
            const place = pickupAutocomplete.getPlace();
            if (!place.geometry) return;

            pickupMap.setCenter(place.geometry.location);
            pickupMarker.setPosition(place.geometry.location);
            fillHiddenFields(place.address_components, place.geometry.location, 'pickup');

            // 📌 Update distance after pickup is selected
            const distance = calculateDistance();
            if (distance !== null) {
                document.getElementById("distance-info").innerText = `Distance: ${distance.toFixed(2)} km`;
            }
            updateDistanceDisplay();
        });

        // === Dropoff Map ===
        dropoffMap = new google.maps.Map(document.getElementById('dropoff-map'), {
            center: defaultLoc,
            zoom: 12
        });

        dropoffMarker = new google.maps.Marker({
            map: dropoffMap,
            position: defaultLoc,
            draggable: true
        });

        dropoffMap.addListener('click', function (event) {
            dropoffMarker.setPosition(event.latLng);
            reverseGeocode(event.latLng, 'dropoff');
        });

        dropoffMarker.addListener('dragend', function (event) {
            reverseGeocode(event.latLng, 'dropoff');
        });

        dropoffAutocomplete = new google.maps.places.Autocomplete(document.getElementById("dropoff-address"));

          dropoffAutocomplete.addListener("place_changed", () => {
                 const place = dropoffAutocomplete.getPlace();
                 if (!place.geometry) return;

                 dropoffMap.setCenter(place.geometry.location);
                 dropoffMarker.setPosition(place.geometry.location);
                 fillHiddenFields(place.address_components, place.geometry.location, 'dropoff');

                 // 📌 Update distance after dropoff is selected
                 const distance = calculateDistance();
                 if (distance !== null) {
                     document.getElementById("distance-info").innerText = `Distance: ${distance.toFixed(2)} km`;
                 }
                 updateDistanceDisplay();
             });
         }

        function reverseGeocode(latlng, type) {
            const geocoder = new google.maps.Geocoder();
            geocoder.geocode({location: latlng}, (results, status) => {
                if (status === "OK" && results[0]) {
                    document.getElementById(`${type}-address`).value = results[0].formatted_address;
                    fillHiddenFields(results[0].address_components, latlng, type);
                }
            });
        }

        function getComponent(components, types) {
            for (let type of types) {
                const comp = components.find(c => c.types.includes(type));
                if (comp) return comp.long_name;
            }
            return '';
        }

        function fillHiddenFields(components, location, prefix) {
            document.getElementById(`${prefix}_city`).value = getComponent(components, ["locality", "administrative_area_level_2"]);
            document.getElementById(`${prefix}_state`).value = getComponent(components, ["administrative_area_level_1"]);
            document.getElementById(`${prefix}_country`).value = getComponent(components, ["country"]);
            document.getElementById(`${prefix}_street`).value = getComponent(components, ["route", "street_address"]);
            document.getElementById(`${prefix}_postal_code`).value = getComponent(components, ["postal_code"]);
            document.getElementById(`${prefix}_latitude`).value = location.lat();
            document.getElementById(`${prefix}_longitude`).value = location.lng();
        }

        function calculateDistance() {
            const pickupLat = parseFloat(document.getElementById("pickup_latitude").value);
            const pickupLng = parseFloat(document.getElementById("pickup_longitude").value);
            const dropoffLat = parseFloat(document.getElementById("dropoff_latitude").value);
            const dropoffLng = parseFloat(document.getElementById("dropoff_longitude").value);

            if (isNaN(pickupLat) || isNaN(pickupLng) || isNaN(dropoffLat) || isNaN(dropoffLng)) {
                return null;
            }

            const pickup = new google.maps.LatLng(pickupLat, pickupLng);
            const dropoff = new google.maps.LatLng(dropoffLat, dropoffLng);
            const distanceInMeters = google.maps.geometry.spherical.computeDistanceBetween(pickup, dropoff);
            return distanceInMeters / 1000; // in kilometers
        }

        function updateDistanceDisplay() {
            const distance = calculateDistance();
            const info = document.getElementById("distance-info");

            if (distance !== null) {
                info.innerText = `Distance: ${distance.toFixed(2)} km`;
            } else {
                info.innerText = "Select both pickup and dropoff locations.";
            }
        }



</script>
-->
<!--

<script>
    let pickupMap, dropoffMap;
    let pickupMarker, dropoffMarker;
    let pickupAutocomplete, dropoffAutocomplete;

    function initMap() {
        const defaultLoc = { lat: 33.8938, lng: 35.5018 }; // Beirut

        // === Pickup Map ===
        pickupMap = new google.maps.Map(document.getElementById('pickup-map'), {
            center: defaultLoc,
            zoom: 12
        });

        pickupMarker = new google.maps.Marker({
            map: pickupMap,
            position: defaultLoc,
            draggable: true
        });

        pickupMap.addListener('click', function (event) {
            pickupMarker.setPosition(event.latLng);
            reverseGeocode(event.latLng, 'pickup');
        });

        pickupMarker.addListener('dragend', function (event) {
            reverseGeocode(event.latLng, 'pickup');
        });

        pickupAutocomplete = new google.maps.places.Autocomplete(document.getElementById("pickup-address"));
        pickupAutocomplete.addListener("place_changed", () => {
            const place = pickupAutocomplete.getPlace();
            if (!place.geometry) return;

            pickupMap.setCenter(place.geometry.location);
            pickupMarker.setPosition(place.geometry.location);
            fillHiddenFields(place.address_components, place.geometry.location, 'pickup');
        });

        // === Dropoff Map ===
        dropoffMap = new google.maps.Map(document.getElementById('dropoff-map'), {
            center: defaultLoc,
            zoom: 12
        });

        dropoffMarker = new google.maps.Marker({
            map: dropoffMap,
            position: defaultLoc,
            draggable: true
        });

        dropoffMap.addListener('click', function (event) {
            dropoffMarker.setPosition(event.latLng);
            reverseGeocode(event.latLng, 'dropoff');
        });

        dropoffMarker.addListener('dragend', function (event) {
            reverseGeocode(event.latLng, 'dropoff');
        });

        dropoffAutocomplete = new google.maps.places.Autocomplete(document.getElementById("dropoff-address"));
        dropoffAutocomplete.addListener("place_changed", () => {
            const place = dropoffAutocomplete.getPlace();
            if (!place.geometry) return;

            dropoffMap.setCenter(place.geometry.location);
            dropoffMarker.setPosition(place.geometry.location);
            fillHiddenFields(place.address_components, place.geometry.location, 'dropoff');
        });
    }

    function reverseGeocode(latlng, type) {
        const geocoder = new google.maps.Geocoder();
        geocoder.geocode({ location: latlng }, (results, status) => {
            if (status === "OK" && results[0]) {
                document.getElementById(`${type}-address`).value = results[0].formatted_address;
                fillHiddenFields(results[0].address_components, latlng, type);
            }
        });
    }

    function getComponent(components, types) {
        for (let type of types) {
            const comp = components.find(c => c.types.includes(type));
            if (comp) return comp.long_name;
        }
        return '';
    }

    function fillHiddenFields(components, location, prefix) {
        document.getElementById(`${prefix}_city`).value = getComponent(components, ["locality", "administrative_area_level_2"]);
        document.getElementById(`${prefix}_state`).value = getComponent(components, ["administrative_area_level_1"]);
        document.getElementById(`${prefix}_country`).value = getComponent(components, ["country"]);
        document.getElementById(`${prefix}_street`).value = getComponent(components, ["route", "street_address"]); // Added
        document.getElementById(`${prefix}_postal_code`).value = getComponent(components, ["postal_code"]); // Added
        document.getElementById(`${prefix}_latitude`).value = location.lat();
        document.getElementById(`${prefix}_longitude`).value = location.lng();
    }
    fillHiddenFields(place.address_components, place.geometry.location, 'dropoff');

    // 📌 Add this to update distance after dropoff is selected
    const distance = calculateDistance();
    if (distance !== null) {
        document.getElementById("distance-info").innerText = `Distance: ${distance.toFixed(2)} km`;
    }
    fillHiddenFields(place.address_components, place.geometry.location, 'pickup');

    // 📌 Add this to update distance after pickup is selected
    const distance = calculateDistance();
    if (distance !== null) {
        document.getElementById("distance-info").innerText = `Distance: ${distance.toFixed(2)} km`;
    }

    function calculateDistance() {
        const pickupLat = parseFloat(document.getElementById("pickup_latitude").value);
        const pickupLng = parseFloat(document.getElementById("pickup_longitude").value);
        const dropoffLat = parseFloat(document.getElementById("dropoff_latitude").value);
        const dropoffLng = parseFloat(document.getElementById("dropoff_longitude").value);

        if (!pickupLat || !pickupLng || !dropoffLat || !dropoffLng) return null;

        const pickupLocation = new google.maps.LatLng(pickupLat, pickupLng);
        const dropoffLocation = new google.maps.LatLng(dropoffLat, dropoffLng);

        const distanceInMeters = google.maps.geometry.spherical.computeDistanceBetween(pickupLocation, dropoffLocation);
        const distanceInKm = distanceInMeters / 1000;

        return distanceInKm; // distance in kilometers
    }

    window.initMap = initMap;
</script>
-->-->
<!-- Google Maps API
<script>
    let map, marker, autocomplete;

   function initMap( ) {
        const defaultLocation = { lat: 33.8886, lng: 35.4955 };
        map = new google.maps.Map(document.getElementById("map-container"), {
            center: defaultLocation,
            zoom: 12,
        });

        marker = new google.maps.Marker({
            position: defaultLocation,
            map: map,
            draggable: true
        });

        const input = document.getElementById("address-search");
        autocomplete = new google.maps.places.Autocomplete(input);

        autocomplete.addListener("place_changed", () => {
            const place = autocomplete.getPlace();
            if (!place.geometry) return;

            map.setCenter(place.geometry.location);
            marker.setPosition(place.geometry.location);
            fillHiddenFields(place.address_components, place.geometry.location);
        });

        map.addListener("click", function (event) {
            const clickedLocation = event.latLng;
            marker.setPosition(clickedLocation);
            reverseGeocode(clickedLocation);
        });
    }

    function reverseGeocode(latlng) {
        const geocoder = new google.maps.Geocoder();
        geocoder.geocode({ location: latlng }, (results, status) => {
            if (status === "OK" && results[0]) {
                // Fill the text input with the formatted address
                document.getElementById("address-search").value = results[0].formatted_address;

                // Fill hidden/read-only fields
                fillHiddenFields(results[0].address_components, latlng);
            } else {
                console.warn("Geocoder failed: ", status);
            }
        });
    }

    function getComponent(components, types) {
        for (let type of types) {
            const comp = components.find(c => c.types.includes(type));
            if (comp) return comp.long_name;
        }
        return '';
    }

    function fillHiddenFields(components, location) {
        document.getElementById("city").value = getComponent(components, ["locality", "administrative_area_level_2"]);
        document.getElementById("state").value = getComponent(components, ["administrative_area_level_1"]);
        document.getElementById("country").value = getComponent(components, ["country"]);
        document.getElementById("latitude").value = location.lat();
        document.getElementById("longitude").value = location.lng();
    }

    window.initMap = initMap;
</script>
 -->
<!-- Mirrored from p.w3layouts.com/demos_new/template_demo/06-11-2024/shipper-liberty-demo_Free/1152162366/web/index.html by HTTrack Website Copier/3.x [XR&CO'2014], Thu, 01 May 2025 18:06:48 GMT -->
<script>
    // Get client's local time when page loads
    document.addEventListener('DOMContentLoaded', function() {
        const clientTime = new Date();

        // Send to Laravel backend
        fetch('/store-client-time', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'X-CSRF-TOKEN': '{{ csrf_token() }}'
            },
            body: JSON.stringify({
                time: clientTime.toISOString(),
                timezone: Intl.DateTimeFormat().resolvedOptions().timeZone
            })
        });
    });
</script>
</html>
